<%# btn= document.querySelector(".attend-btn");
btn.addEventListener("click", function(e){
e.preventDefault();
if (btn.innerHTML === "ATTEND"){
    btn.innerHTML = "Attending"
} else{
    btn.innerHTML === "ATTEND"
}
}) %>